%p
  #{t('welcome', name: @email)}!

%p
  = t('devise.mailer.confirmation_instructions.body_description')

%p
  = link_to t('devise.mailer.confirmation_instructions.link'), confirmation_url(@resource, confirmation_token: @token)
